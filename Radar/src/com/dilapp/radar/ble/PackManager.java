package com.dilapp.radar.ble;

public class PackManager {

/*	private static PackManager mPackManager  = null;
	private static Object object = new Object();
	
	private PackManager(){
		
	}
	public static PackManager getInstance(){
		synchronized (object) {
			if(mPackManager == null){
				mPackManager = new PackManager();
			}
			return mPackManager;
		}
	}
	*/
	 
	
	
	
	
}
